{
  "name": "jkif",
  "version": "0.0.1",
  "scripts": {
    "test": "for f in test/specs/*; do echo \"$f\"; mocha \"$f\"; done; exit 0"
  },
  "devDependencies": {
    "chai": "^2.2.0",
    "mocha": "^2.2.1"
  },
  "dependencies": {
    "jison": "^0.4.15",
    "jsonfile": "^2.0.0"
  }
}
